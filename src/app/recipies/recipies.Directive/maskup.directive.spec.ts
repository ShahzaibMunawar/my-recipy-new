import {MaskupDirective} from './maskup.directive';

describe('MaskupDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskupDirective();
    expect(directive).toBeTruthy();
  });
});
